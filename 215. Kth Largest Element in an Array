class Solution {
public:
    int findKthLargest(vector<int>& nums, int k) {
        int n=nums.size()-1;
        int large=0;
        sort(nums.begin(),nums.end());
       
      return nums[n-k+1];
    }
};
